# exif_tool
fammiuna breve descrizione non md  21:48 EXIF metadata extractor for OSINT. Recovers device info (make, model, lens, serial), capture settings (shutter, aperture, ISO, focal length), GPS coordinates with Google Maps and OpenStreetMap links, reverse geocoded address, author data and copyright. Supports batch analysis and JSON output. 
